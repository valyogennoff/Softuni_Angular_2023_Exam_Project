import { PassDirective } from './pass.directive';

describe('PassDirective', () => {
  it('should create an instance', () => {
    const directive = new PassDirective();
    expect(directive).toBeTruthy();
  });
});
